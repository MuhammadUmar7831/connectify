import { Router } from "express";

const router = Router();

// router.post('/signup', authenticate, tryCatch(signup)); //route for sign up
// router.post('/signin', authenticate, tryCatch(signin)); //route for sign in
// router.delete('/signout/:userId', authenticate, tryCatch(signout)); //route for sign in
// router.get('/get', authenticate, tryCatch(getUser)); //route for getting user

export default router;